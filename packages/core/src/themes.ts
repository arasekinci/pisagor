import type { Theme } from './types'
import { N300, N500 } from './colors'

export const light: Theme = {
  breadcrumbs: {
    color: N300,
    hoverColor: N500,
  },
}

export const dark: Theme = {
  breadcrumbs: {
    color: N300,
    hoverColor: N500,
  },
}
